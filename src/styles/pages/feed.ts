import { styled } from "@/stitches.config";
import Image from "next/image";

export const ConteinerFeed = styled('div', {
    display:'flex',
    flexDirection:'row',
})
export const PageTitleImage = styled(Image, {

})
export const PageTitleContent =styled('div', {
    display:'flex',
    flexDirection:'row',

})

export const TitlePage = styled('div',{
    display:'flex',
    flexDirection:'row',
    justifyContent:'space-between',
})

export const TitleTranding = styled('div', {
    display:'flex',
    flexDirection:'row',
    justifyContent:'space-between',
})